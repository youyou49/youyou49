	int n = 8;
		unsigned int sz = sizeof(n)
		int bit_num = sz * 8
		int sum = 0;
		int i = 0;
			for (i = 0; i < bit_num; i++)
			{
				if (1 & (n>>i))
					sum++;
			}
			printf("%d\n", sum);
		// 01000
		// 00001
